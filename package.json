{
  "name": "upgrade-siren",
  "version": "0.0.0",
  "private": true,
  "description": "Upgrade Siren — public upgrade-risk alarm for Ethereum contracts.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "provision-ens": "tsx scripts/provision-ens.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "viem": "^2.21.45",
    "@types/node": "^22.10.0"
  }
}
